"use strict";


// todo probably a json definition
export default class UserModel {
    /**
     *
     * @param id
     * @param name
     */
    constructor({id, name}) {
        this.id = id;
        this.name = name;
    }
}